<Window x:Class="FileReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileReader"
        mc:Ignorable="d"        
        d:DataContext="{d:DesignInstance local:MainViewModel}"
        Background="#FF2F2E2E"  Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            
            <Button Height="30" Width="100" Command="{Binding OpenCommand}" > Open File </Button>
            <Button Height="30" Width="100" Command="{Binding SaveCommand}" > SaveFile  </Button>
           
        </StackPanel>
    </Grid>
</Window>
